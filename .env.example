# 复制为 .env 并填入你的密钥（.env 已被 .gitignore 忽略，不会上传）
# 换 API 节点 / 换模型 = 只改这个文件，所有 step 的代码一行不动

LLM_API_KEY=sk-ss-v1-你的密钥
LLM_BASE_URL=https://zenmux.ai/api/v1
LLM_MODEL=z-ai/glm-5.3-flashx

# zenmux 直连不通，需要走本机代理（按需修改；不需要代理就删掉这行）
LLM_PROXY=http://127.0.0.1:7897
